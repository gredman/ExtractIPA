Extract IPA.workflow
====================

Finder Quick Action to extract the payload from an iOS IPA file.

Installation
------------

Copy `Extract IPA.workflow` into `$HOME/Library/Services`.

Usage
-----

Right-click an IPA and select `Extract IPA`.